# Application bindings
$terminal = uwsm app -- $TERMINAL
$browser = omarchy-launch-browser

bindd = SUPER, return, Terminal, exec, $terminal --working-directory="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER, F, File manager, exec, uwsm app -- nautilus --new-window
bindd = SUPER, B, Browser, exec, $browser
bindd = SUPER SHIFT, B, Browser (private), exec, $browser --private
bindd = SUPER, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER, N, Editor, exec, omarchy-launch-editor
bindd = SUPER, T, Activity, exec, $terminal -e btop
bindd = SUPER, D, Docker, exec, $terminal -e lazydocker
bindd = SUPER, G, GitKraken, exec, uwsm app -- gitkraken
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus obsidian "uwsm app -- obsidian -disable-gpu --enable-wayland-ime"
bindd = SUPER, slash, Passwords, exec, uwsm app -- 1password

# If your web app url contains #, type it as ## to prevent hyperland treat it as comments
bindd = SUPER, C, Cursor, exec, uwsm app -- cursor
bindd = SUPER, E, File manager, exec, uwsm app -- nautilus --new-window
# Clipboard Manager
bindd = SUPER, Y, Clipboard Manager, exec, omarchy-launch-walker -m clipboard

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu

# Focus movement with vim keys (new functionality)
unbind = SUPER, J
unbind = SUPER, K
bindd = SUPER, h, Move focus left, movefocus, l
bindd = SUPER, l, Move focus right, movefocus, r
bindd = SUPER, j, Move focus down, movefocus, d
bindd = SUPER, k, Move focus up, movefocus, u

# Move conflicting keybindings to new locations
bindd = SUPER, I, Toggle split, togglesplit,
bindd = SUPER, U, Show key bindings, exec, omarchy-menu-keybindings

# Window movement with vim keys (replaces arrow key swapping)
bindd = SUPER SHIFT, h, Move window left, movewindow, l
bindd = SUPER SHIFT, l, Move window right, movewindow, r  
bindd = SUPER SHIFT, j, Move window down, movewindow, d
bindd = SUPER SHIFT, k, Move window up, movewindow, u

# Window resizing with vim keys (new functionality)
bindd = SUPER ALT, h, Reduce window width, resizeactive, -100 0
bindd = SUPER ALT, l, Increase window width, resizeactive, 100 0
bindd = SUPER ALT, j, Increase window height, resizeactive, 0 100
bindd = SUPER ALT, k, Reduce window height, resizeactive, 0 -100

# Archivist - AI Prompts Manager
bindd = SUPER, A, Archivist, exec, ~/.config/hypr/scripts/archivist.sh

# Keyboard Layout Switcher
bindd = SUPER, O, Keyboard layout, exec, ~/.config/hypr/scripts/keyboard-layout.sh

# Window management
unbind = SUPER, W
bindd = SUPER, Q, Close active window, killactive


